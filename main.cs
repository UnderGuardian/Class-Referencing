using System;
using Newtonsoft.Json;
using System.IO;
using System.Net;
using System.Web;
using System.Web.Script.Serialization;

class Program{
    static void Main(string[] args)
    { /*
      //declarar clase (osea, poner el nombre de "class")
        metodos obj = new metodos();
      //luego para proyectarlo usar obj + un punto + el metodo que quieras usar + entre parentesis los argumentos/valores que hayas puesto a ese metodo
        Console.WriteLine(obj.suma(4, 65));
        obj.imprimirMensaje(); */
  } //here ends method
} //here ends class